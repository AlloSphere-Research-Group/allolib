# write comment with #
my_float = 13

# start table with []
[my_table]
my_int = 3 # `my_int` will be in `my_table` table

my_str = "my string" # also in `my_table` table
                     # table has scope till other table's beginning
                     # or till end of file
